import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'lump.da.skump.empty.capacitor',
  appName: 'capacitor empty',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
